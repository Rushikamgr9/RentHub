{
  "name": "roughrenthub",
  "version": "1.0.0",
  "private": true,
  "description": "RentHub — JWT auth, role-based access. One command to run.",
  "scripts": {
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "backend": "cd backend && node server.js",
    "frontend": "cd frontend && npm start",
    "dev": "npx concurrently -k -n api,web -c cyan,magenta \"npm run backend\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
